package matrix_test

import (
	"testing"
	m"vectris/matrix"
)

/*

func TestEqual(t *testing.T) {
	type args struct {
		a *m.Matrix[N]
		b *m.Matrix[N]
	}
	tests := []struct {
		name string
		args args
		want bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			if got := m.Equal(tt.args.a, tt.args.b); got != tt.want {
				t.Errorf("Equal() = %v, want %v", got, tt.want)
			}
		})
	}
}


func BenchmarkEqual(b *testing.B) {
	type def struct{
		mat[N] [][]N
	}
	tests := []struct{
		name string
		a	*m.Matrix[N]
		b	*m.Matrix[N]
		value bool
		wanterr bool
	}{
		{"Small Matrix same type", m.MatrixUnsigned(10, 10), m.MatrixUnsigned(10, 10), true, false},

	}

}
*/
